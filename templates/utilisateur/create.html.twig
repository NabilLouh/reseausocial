{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{form_widget(form)}}

        <button formnovalidate>
            Creer l'utilisateur
        </button>
    {{form_end(form)}}
{% endblock %}